{
  "name": "@space-launch-tracking-app/shared-types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for the Space Launch Tracking application",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf node_modules dist .turbo",
    "type-check": "tsc --noEmit",
    "prepublishOnly": "npm run clean && npm run build",
    "lint": "eslint . --max-warnings 0"
  },
  "keywords": [
    "typescript",
    "types",
    "space",
    "launch",
    "tracker"
  ],
  "author": "Robert Rojas <@rrojasf> rbrt.rojas@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.6.2",
    "@space-launch-tracking-app/eslint-config": "*"
  },
  "peerDependencies": {
    "typescript": "^5.6.2"
  }
}
